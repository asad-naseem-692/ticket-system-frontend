# Feature: Public Reply (UI)
**Owner:** Frontend | **Module:** Comments & Attachments

## Goal
Let anyone on the ticket (customer, agent, admin) send a visible reply.

## Scope
- Default tab in `components/CommentBox.tsx`.
- Shown to all roles. Renders in the shared comment thread on `tickets/[id]/page.tsx`.
